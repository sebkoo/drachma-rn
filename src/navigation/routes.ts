/** The app's routes, in one place so screens can type navigation without
 *  importing App (which would be circular). */
export type RootStackParamList = {
  Converter: undefined;
  Alerts: undefined;
  AboutRates: undefined;
};
